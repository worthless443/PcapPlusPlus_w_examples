#include<string>
#include<vector>

int indx(std::string main, std::string substr, int idx);
const std::vector<std::string> parseIpv4(const char *addr);

//expand it in future with macro for saftely and features
